# DateFiler 開発議事録

---

## 2026-07-28 〜 2026-07-29

### 作業内容

#### 1. カスタムタイトルバーの実装
- Windows OSのタイトルバーをFigmaデザインのヘッダーバーに置き換え
- `Qt.WindowType.FramelessWindowHint` でフレームレス化
- `CustomTitleBar(QWidget)` クラスを実装（ドラッグ移動・最小化・最大化・閉じるボタン付き）
- ヘッダー背景色: `#FFFFFF`（白）

#### 2. フォルダ編集ダイアログのフレームレス化
- `FolderEditDialog` に `FramelessWindowHint` を適用
- OSタイトルバー除去により「フォルダの編集」の二重表示を解消

#### 3. タイルスタイルの調整
- 編集モード時のタイルに白カード背景（`#FFFFFF`）を適用
- リンクのみタイル（`allow_filemove=False`）に通常モードでグレーカード背景を適用
- スタイル定数を整理（`_TILE_NORMAL`, `_TILE_LINK_NORMAL`, `_TILE_EDIT` 等）

#### 4. チェックボックスのカスタムデザイン
- `_CheckBox(QCheckBox)` クラスを新規実装
- `paintEvent` をフルオーバーライドし、実際のチェックマーク（✓）を描画
- 未チェック: グレー枠の白背景、チェック済: ダーク背景に白チェックマーク

#### 5. 追加モードオーバーレイの全面再構築
- **課題**: オーバーレイが `GridContainer`（グリッド領域のみ）を覆っており、下部ボトムバー（FABボタン）が見えてしまっていた
- **修正方針**: オーバーレイを `_central`（ウィンドウ全体）の子ウィジェットとして配置
- `GridContainer` からオーバーレイ管理（`set_overlay`・`show_overlay`）を削除
- `AddModeOverlay` を `_central` の子として生成、`_central.rect()` にサイズを合わせて表示
- 右下の × ボタン: 白背景・黒アイコン（オーバーレイの上に浮かぶデザイン）
- 左FAB（編集ボタン）: オーバーレイ下に隠れた状態（操作不可）
- ウィンドウリサイズ時もオーバーレイサイズが追従するよう `resizeEvent` を更新
- `close_requested` シグナルで × ボタンから `_exit_add_mode` を呼び出す

### 確認済み動作
- 追加モード開始: + ボタン押下 → オーバーレイがウィンドウ全体を覆う
- 追加モード終了: オーバーレイ右下の × ボタン押下 → オーバーレイ非表示
- ボトムバーのFABボタンがオーバーレイで隠れる（Figmaデザイン通り）

### ビルド
- `dist\DateFiler.exe` に反映済み

---

## 2026-07-30 〜 2026-07-31

### 作業内容

#### 1. 編集ダイアログへの削除ボタン追加
- `FolderEditDialog` のタイトル行右側に「削除する」ボタンを追加（Figma Redesign_edit_2 準拠）
- ボタン色: 赤 (`#E53935`)、ホバー時: 濃赤 (`#B71C1C`)
- `FolderEditDialog.deleted` フラグで削除意図を返し、`_edit_tile` 内で確認ダイアログを経て削除

#### 2. 並び順をドラッグ＆ドロップ入れ替えに変更
- 従来のクリック数・移動数による自動ソートを廃止 (`_sorted_folders` は登録順をそのまま返す)
- 編集モード中にタイルを左右にドラッグ＆ドロップして並び順を変更できる機能を実装
- カスタム MIME タイプ `application/x-datefiler-tile` にフォルダパスを乗せてやり取り
- `FolderTile.reorder_requested` シグナル (`str, str, bool`) で「移動元パス・移動先パス・後挿入フラグ」を伝達
- `MainWindow._reorder_folder` でインデックスを計算して `settings["folders"]` を入れ替え

#### 3. 最後のタイルのドロップ問題を修正
- ドロップ先がない最後のタイルへの入れ替えができなかった問題を解消
- タイルの左半分ドロップ → 前に挿入 / 右半分ドロップ → 後ろに挿入 の左右判定を導入
- ドロップインジケーター: 左ボーダー青 (`_TILE_DROP_BEFORE`) / 右ボーダー青 (`_TILE_DROP_AFTER`)

#### 4. Figma デザイン準拠の調整
- ファイル移動タイル高さ: `TILE_H = 90`、リンクタイル高さ: `TILE_H_LINK = 48`
- フォルダアイコン色: `FOLDER_YELLOW = "#FFD55F"`
- タイル角丸: `border-radius: 6px`（全スタイル定数を更新）
- リンクタイル通常背景: `#FBF8F8`
- タイルグリッドを上段（ファイル移動可）・下段（リンクのみ）の2セクション構成に変更

#### 5. フォルダアイコンをSVGに変更
- Figma から正確なSVGパスをブラウザ操作でエクスポート
- `_SVG_FOLDER_TILE` 定数としてパスデータを保持
- `make_tile_folder_pixmap` を `QSvgRenderer` 使用に書き換え（再現精度向上）
- バッジ（日付フォルダ用 + マーク）は引き続き QPainter で上乗せ描画

#### 6. バグ修正: 編集モード中にファイルドロップが無効になる問題
- `dragEnterEvent` / `dragMoveEvent` の条件が `not self._edit_mode` だったため、編集モード中はファイル移動可タイルへのファイルドロップが無効になっていた
- `self._allow_filemove` への変更により、ファイル移動可タイルは編集モードに関わらず常にファイルを受け付けるよう修正

### ビルド
- `dist\DateFiler.exe` に反映済み

